
window.config = {
  // GraphQL server URI
  graphqlURI: "https://example.com/graphql",
  // Authorization header for GraphQL request, if necessary. Optional.
  graphqlAuthHeader: "Basic abc123==",
  // URI prefix for aliases. This will be used when copying an alias URL.
  aliasPrefix: "https://exam.pl/"
}